{
  "name": "lens-notifications",
  "version": "1.0.0",
  "description": "Lens Protocol notifications serverless function",
  "scripts": {
    "dev": "vercel dev",
    "lint": "eslint .",
    "deploy": "vercel deploy",
    "deploy:prod": "vercel deploy --prod",
    "env:pull": "vercel env pull",
    "logs": "vercel logs",
    "start": "node api/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "web-push": "^3.6.6"
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "vercel": "^32.5.0"
  },
  "engines": {
    "node": ">=18.x"
  }
}
